# Copy to secrets.py locally; never commit the filled-in file.
# This relay client uses 2.4 GHz Wi-Fi and derives its pairing ID from the chip.
# Its relay host is defined in main.py; there is no LAN-only/token mode here.
WIFI_SSID = "YOUR_WIFI_NAME"
WIFI_PASSWORD = "YOUR_WIFI_PASSWORD"
